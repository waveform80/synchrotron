// Produces a projection of the bottom of the model
projection(cut=true) translate([0,0,1.1]) import("synchrotron.stl");

// Produces a projection of the top of the model
//projection(cut=true) translate([0,0,0]) import("synchrotron.stl");
